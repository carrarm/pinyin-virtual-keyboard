import { KeyLongPress } from './key-long-press';

describe('KeyLongPress', () => {
  it('should create an instance', () => {
    const directive = new KeyLongPress();
    expect(directive).toBeTruthy();
  });
});
